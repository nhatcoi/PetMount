server:
  port: ${SERVER_PORT}
  address: ${SERVER_ADDRESS}

spring:
  application:
    name: PetMount
  profiles:
    active: dev

logging:
  level:
    root: INFO
    web: OFF
    org.springframework: INFO
    org.apache: OFF
    kafka: OFF